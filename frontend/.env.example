# 前端部署到站点根路径时保持 /
VITE_APP_BASE_URL=/

# 默认和 Nginx 反向代理配合，前端同源访问 /api
VITE_API_BASE_URL=/api

# 云端（Vercel + Render）时改为 Render 上的 API 根路径，例如：
# VITE_API_BASE_URL=https://your-api.onrender.com/api

# 仅本地开发时用于 Vite dev server 代理
VITE_DEV_API_TARGET=http://localhost:3001
